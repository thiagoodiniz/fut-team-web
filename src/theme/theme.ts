import type { ThemeConfig } from 'antd'

export const appTheme: ThemeConfig = {
  token: {
    // cor principal do app
    colorPrimary: '#16a34a',

    // usado em links, botões secundários e feedback
    colorInfo: '#16a34a',

    // ajustes globais
    borderRadius: 12,
    fontSize: 14,

    // fundo padrão do layout
    colorBgLayout: '#f6f7f9',
  },
}
